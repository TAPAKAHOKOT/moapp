# Инструкции для разработки

- Вести разработку напрямую в ветке `main` и отправлять изменения в `main`, пока пользователь явно не попросит обратного.
- При создании коммитов всегда следовать соглашению Conventional Commits.
